#include "malloc.h"

/**
 * _realloc - reallocate memory pointed to by @ptr
 *
 * @ptr: pointer to array to resize
 * @size: new size of array
 *
 * Return: pointer to new array space
 */
void *_realloc(void *ptr, size_t size)
{
	return (ptr);
}
